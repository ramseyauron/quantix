# SEC-KEY01: Never commit private key material
# P2P node private keys — generated at runtime, must not be in source control
src/p2p/data/*/keys/private.key
src/p2p/data/*/keys/public.key
src/p2p/data/*/keys/node_info.json
# CLI node keys — generated at runtime (SEC-HIST01)
src/cli/data/*/keys/private.key
src/cli/data/*/keys/public.key
src/cli/data/*/keys/node_info.json

# Validator reward keypair — contains SPHINCS+ private key (SEC-KEY02)
# Generated at first node startup; must never be committed
validator-key.json
**/validator-key.json

# LevelDB / RocksDB data directories
src/p2p/data/*/chaindata/
src/p2p/data/*/state.db/
src/p2p/data/*/blockchain.db/
*.ldb
*.log

# Test binary artifacts
*.test
common.test
http.test
transaction.test

# Build output
/quantix
/quantixd
/bin/

# SPHINCS+ key stores — never commit wallet key material
*.keystore
*.vault

# OS artifacts
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp
*~

# Go
vendor/
